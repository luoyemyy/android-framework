<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="presenter"
            type="com.github.luoyemyy.framework.test.MainActivity.Presenter" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/txtUsername"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{presenter.user.username}"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/txtPassword"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{presenter.user.password}"
            app:layout_constraintTop_toBottomOf="@id/txtUsername" />

        <CheckBox
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onCheckedChanged="@{(v,checked)->presenter.check(v,checked,presenter.user)}"
            android:text="checkbox"
            app:layout_constraintTop_toBottomOf="@id/txtPassword" />

    </android.support.constraint.ConstraintLayout>
</layout>
